import React from "react";

const Precios = () => {
  // const preciosRef = useRef(null);

  return (
    <section
      // ref={preciosRef}
      id="precios"
      className="min-h-[600px] bg-green-300"
    >
      Esta es la seccion de PRecios
    </section>
  );
};

export default Precios;
